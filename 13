#include <stdio.h>
#include <algorithm>
using namespace std;
int n,a[30000],i,t,b;
int main()
{
	scanf("%d",&n);
	for (i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	sort(a,a+n);
	scanf("%d",&t);
	for (i=0;i<t;i++)
	{
		scanf("%d",&b);
		printf("%d\n", a[b-1]);
	}
	return 0;
}
