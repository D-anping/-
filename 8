#include <stdio.h>
int n,i;
int main()
{
	n=0;
	for (i=1000;i<=1100;i++)
	{
		if (i%3==2 && i%5==3 && i%7==2 && n!=0)
		{
			printf(" %d\n",i);
		}
		if (i%3==2 && i%5==3 && i%7==2 && n==0)
		{
			printf("%d\n",i);
			n++;
		}

	}
	return 0;
}
