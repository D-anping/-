#include <stdio.h>
float a,sum;
int i;
int main()
{
	while (scanf("%f",&a)!=EOF && a!=0)
	{
		sum=0.5;
		i=2;
		while (sum<a)
		{
			i++;
			sum+=1.0/i;
		}
		printf("%d card(s)\n",i-1);
	}
	return 0;
}
